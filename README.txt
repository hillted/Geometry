Idea Project, built with Java8 on Mac osx.

To Build And Run Tests...will create files.
--------------------------------------------
 cd ~/IdeaProjects/Geometry #or wherever the project is installed.
 mvn clean install

 Has some shapes that override getArea, *Tests walk thru the process.